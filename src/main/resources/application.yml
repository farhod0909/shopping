spring:
  datasource:
    username: postgres
    password: farhod1994
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/postgres
  jpa:
    hibernate:
      ddl-auto: update
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
security:
  oauth2:
    resourceserver:
      jwt:
        signing-key: '4u7bhjasbhjsjbMcQfT'